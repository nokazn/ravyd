{
  "extends": "./tsconfig-base.json",
  "compilerOptions": {
    "lib": [
      "ESNext",
      "ESNext.AsyncIterable",
      "DOM"  // .vue ファイルはこの設定ファイルしか読み取らない?
    ],
    "baseUrl": "./"
  },
  "exclude": [
    "node_modules"
  ],
}
